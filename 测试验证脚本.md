# 前端Agent功能测试验证脚本

## 测试环境准备

### 1. 启动后端服务

```bash
cd D:\lqh\金融
python -m uvicorn app.WealthButler.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 准备测试账号

根据 `docs/测试账号.md`，使用以下账号：

**员工账号（advisor角色）**：
- 用户名：`advisor_wang`
- 密码：`password123`

**员工账号（operator角色）**：
- 用户名：`operator_li`
- 密码：`password123`

**客户账号**：
- 用户名：`customer_zhang`
- 密码：`password123`

## 测试用例

### 测试1：后端API直接测试

#### 1.1 获取JWT Token

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "advisor_wang", "password": "password123"}'
```

**预期输出**：
```json
{
  "code": 0,
  "data": {
    "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "token_type": "bearer"
  }
}
```

**提取token**：复制 `access_token` 的值，替换下面命令中的 `<TOKEN>`

#### 1.2 测试投顾Agent（不传customer_id）

```bash
curl -X POST http://localhost:8000/api/chat/advisor \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"message": "近期有什么稳健型基金推荐吗？", "session_id": "test_advisor_123", "user_id": 1, "is_stream": true}'
```

**预期结果**：
- HTTP状态码：200
- Content-Type: text/event-stream
- 返回SSE格式数据流

#### 1.3 测试业务操作Agent（不传customer_id）

```bash
curl -X POST http://localhost:8000/api/chat/operator \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"message": "请问客户信息更新的流程是什么？", "session_id": "test_operator_456", "user_id": 1, "is_stream": true}'
```

**预期结果**：
- HTTP状态码：200
- Content-Type: text/event-stream
- 返回SSE格式数据流

#### 1.4 测试客服Agent

```bash
curl -X POST http://localhost:8000/api/chat/customer \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"message": "我想了解理财产品的风险等级", "session_id": "test_customer_789", "user_id": 1, "is_stream": true}'
```

**预期结果**：
- HTTP状态码：200
- Content-Type: text/event-stream
- 返回SSE格式数据流

### 测试2：前端页面功能测试

#### 2.1 advisor_dashboard.html 测试

**访问地址**：`http://localhost:8000/advisor_dashboard.html`

**测试步骤**：

1. **登录**
   - 使用 `advisor_wang` / `password123` 登录
   - ✅ 检查点：登录成功，跳转到工作台

2. **页面加载检查**
   - ✅ 检查点：页面正常显示，不是空白页
   - ✅ 检查点：顶部导航栏显示用户名"王顾问"
   - ✅ 检查点：统计卡片区域显示（今日服务客户数、完成交易笔数、待处理工单数）
   - ✅ 检查点：待办工单列表显示
   - ✅ 检查点：今日客户列表显示

3. **AI助手功能测试**
   - 点击右下角AI助手气泡（🤖图标）
   - ✅ 检查点：聊天窗口弹出
   - ✅ 检查点：显示欢迎消息
   - ✅ 检查点：有两个Agent切换标签："投顾助手"和"业务操作"

4. **投顾助手对话测试**
   - 确保"投顾助手"标签被选中（蓝色背景）
   - 在输入框输入："请推荐一些适合保守型客户的理财产品"
   - 点击"发送"按钮或按Enter键
   - ✅ 检查点：用户消息立即显示在聊天窗口
   - ✅ 检查点：AI回复逐字流式显示
   - ✅ 检查点：无错误提示
   - 打开浏览器开发者工具（F12），查看Console标签
   - ✅ 检查点：无红色错误日志

5. **业务操作助手对话测试**
   - 点击"业务操作"标签
   - 在输入框输入："如何帮客户办理基金赎回？"
   - 点击"发送"
   - ✅ 检查点：用户消息立即显示
   - ✅ 检查点：AI回复流式显示
   - ✅ 检查点：无错误提示

6. **连续对话测试**
   - 继续发送："赎回需要几个工作日到账？"
   - ✅ 检查点：AI能够理解上下文，继续回答
   - ✅ 检查点：聊天历史保留

7. **Agent切换测试**
   - 切换回"投顾助手"标签
   - ✅ 检查点：显示之前的投顾助手对话历史
   - 切换到"业务操作"
   - ✅ 检查点：显示业务操作助手的对话历史
   - ✅ 检查点：两个Agent的对话历史互不干扰

#### 2.2 operator_dashboard.html 测试

**访问地址**：`http://localhost:8000/operator_dashboard.html`

**测试步骤**：

1. **登录**
   - 使用 `operator_li` / `password123` 登录
   - ✅ 检查点：登录成功

2. **页面加载检查**
   - ✅ 检查点：页面正常显示，不是空白页
   - ✅ 检查点：顶部显示"业务助手工作台"
   - ✅ 检查点：统计卡片显示（今日处理数、待处理、本月客户数、平均用时）
   - ✅ 检查点：工单列表显示
   - ✅ 检查点：右侧AI助手面板显示

3. **AI助手对话测试**
   - 在右侧AI助手输入框输入："请介绍一下转账审核的流程"
   - 点击"发送"或按Enter
   - ✅ 检查点：消息发送成功
   - ✅ 检查点：AI回复正常显示（可能是alert弹窗形式）
   - ✅ 检查点：Console无错误日志

4. **工单筛选测试**
   - 点击不同的筛选标签（全部、客户转介、信息变更、转账审核）
   - ✅ 检查点：工单列表根据筛选条件更新

#### 2.3 customer_dashboard.html 测试

**访问地址**：`http://localhost:8000/customer_dashboard.html`

**测试步骤**：

1. **登录**
   - 使用 `customer_zhang` / `password123` 登录
   - ✅ 检查点：登录成功

2. **页面加载检查**
   - ✅ 检查点：页面正常显示
   - ✅ 检查点：顶部导航栏显示客户名
   - ✅ 检查点：统计卡片显示（总资产、今日收益、持仓数量）
   - ✅ 检查点：持仓表格显示（可能是真实数据或Mock数据）

3. **AI聊天功能测试**
   - 点击右下角聊天气泡（💬图标）
   - ✅ 检查点：聊天窗口弹出
   - ✅ 检查点：显示欢迎消息
   - 在输入框输入："我想了解我的持仓收益情况"
   - 点击发送（➤按钮）
   - ✅ 检查点：用户消息显示
   - ✅ 检查点：AI客服回复显示
   - ✅ 检查点：Console无错误日志

4. **持仓数据刷新测试**
   - 点击"刷新数据"按钮
   - ✅ 检查点：加载动画显示
   - ✅ 检查点：数据重新加载

### 测试3：错误处理验证

#### 3.1 模拟后端服务不可用

1. 停止后端服务（Ctrl+C）
2. 在前端页面尝试发送AI消息
3. ✅ 检查点：显示友好错误提示
4. ✅ 检查点：页面不崩溃，其他功能仍可用
5. 打开Console查看
6. ✅ 检查点：有详细错误日志（包含状态码和错误详情）

#### 3.2 模拟网络延迟

1. 重启后端服务
2. 在Chrome DevTools中打开Network标签
3. 设置网络节流（Throttling）为"Slow 3G"
4. 发送AI消息
5. ✅ 检查点：消息仍能正常发送
6. ✅ 检查点：AI回复流式显示（虽然速度慢）
7. ✅ 检查点：无超时错误

### 测试4：浏览器兼容性测试

在以下浏览器中重复测试2的步骤：

- ✅ Chrome（推荐版本）
- ✅ Edge
- ✅ Firefox
- ⚠️ Safari（如果可用）

## 回归测试清单

修复后需要确认以下功能未受影响：

### advisor_dashboard.html
- ✅ 工单列表加载
- ✅ 工单筛选（全部、客户转介、风险预警）
- ✅ 工单领取功能
- ✅ 客户列表显示
- ✅ 统计数据加载
- ✅ 用户下拉菜单
- ✅ 退出登录

### operator_dashboard.html
- ✅ 工单列表加载
- ✅ 工单筛选（全部、客户转介、信息变更、转账审核）
- ✅ 工单领取功能
- ✅ 最近服务客户列表
- ✅ 统计数据加载
- ✅ 快速操作按钮（虽然功能待开发）

### customer_dashboard.html
- ✅ 持仓数据加载（真实API或Mock降级）
- ✅ 统计数据计算
- ✅ 持仓表格渲染
- ✅ 数据刷新功能
- ✅ 用户信息显示

## 性能测试

### 响应时间测试

1. 打开Chrome DevTools > Network标签
2. 发送AI消息
3. 查看 `/api/chat/advisor` 或 `/api/chat/operator` 请求
4. ✅ 检查点：首字节时间（TTFB）< 2秒
5. ✅ 检查点：流式响应开始时间 < 3秒

### 内存泄漏测试

1. 打开Chrome DevTools > Performance标签
2. 开始录制
3. 连续发送20条AI消息
4. 停止录制
5. ✅ 检查点：内存使用稳定，无明显增长趋势

## 测试结果记录

### 测试执行日期
- [ ] 测试执行日期：________

### 测试人员
- [ ] 测试人员：________

### 测试通过标准
- [ ] 所有核心功能测试通过（测试2）
- [ ] 至少80%的检查点通过
- [ ] 无P0级别bug（页面崩溃、功能完全不可用）
- [ ] 错误处理正常（测试3）

### 发现的问题

| 序号 | 问题描述 | 严重程度 | 复现步骤 | 状态 |
|------|----------|----------|----------|------|
| 1    |          |          |          |      |
| 2    |          |          |          |      |
| 3    |          |          |          |      |

### 备注

_记录任何特殊情况、环境依赖、或需要注意的事项_

---

## 快速验证命令

如果只想快速验证修复是否生效，执行以下步骤：

```bash
# 1. 启动服务
cd D:\lqh\金融
python -m uvicorn app.WealthButler.main:app --reload --host 0.0.0.0 --port 8000

# 2. 在另一个终端，执行快速测试
# 首先登录获取token
TOKEN=$(curl -s -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "advisor_wang", "password": "password123"}' \
  | grep -o '"access_token":"[^"]*"' \
  | cut -d'"' -f4)

echo "Token: $TOKEN"

# 测试投顾Agent（不传customer_id）
echo "测试投顾Agent..."
curl -X POST http://localhost:8000/api/chat/advisor \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "推荐基金", "session_id": "test1", "user_id": 1, "is_stream": true}'

echo ""
echo "---"

# 测试业务操作Agent（不传customer_id）
echo "测试业务操作Agent..."
curl -X POST http://localhost:8000/api/chat/operator \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "办理流程", "session_id": "test2", "user_id": 1, "is_stream": true}'
```

**预期结果**：
- 两个请求都返回200状态码
- 返回SSE格式的流式数据
- 无400错误（customer_id校验失败）

## 前端快速验证

1. 打开浏览器访问：`http://localhost:8000/advisor_dashboard.html`
2. 登录（advisor_wang / password123）
3. ✅ **关键检查**：页面是否正常显示（不是空白）
4. 点击右下角AI助手气泡
5. 发送任意消息
6. ✅ **关键检查**：AI是否有回复（即使是"正在思考..."）
7. 打开F12开发者工具 > Console
8. ✅ **关键检查**：无红色错误日志

如果以上3个关键检查都通过，说明核心问题已修复。
