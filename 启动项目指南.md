# 智能财富管家系统 - 快速启动指南

**当前时间**：2026-08-16  
**项目状态**：✅ 5个Agent已集成，代码已提交，准备启动测试

---

## 一、启动前准备工作

### 1.1 检查Python环境
```bash
# 当前Python版本：3.13.5 ✅
python --version
```

### 1.2 检查依赖服务状态

**必需服务**（启动前必须运行）：
- ✅ **MySQL** (端口3306) - 核心数据库
- ✅ **Redis** (端口6379) - 事件总线+缓存

**可选服务**（部分功能需要）：
- ⚠️ **Milvus** (端口19530) - RAG知识库检索（客服Agent）
- ⚠️ **Neo4j** (端口7687) - 知识图谱（投顾Agent）
- ⚠️ **Ollama** (端口11434) - 本地向量模型（RAG嵌入）

### 1.3 检查服务运行状态

**Windows系统检查**：
```bash
# 检查MySQL
netstat -ano | findstr 3306

# 检查Redis
netstat -ano | findstr 6379

# 检查Milvus
netstat -ano | findstr 19530

# 检查Neo4j
netstat -ano | findstr 7687
```

**Linux/Mac检查**：
```bash
# 检查MySQL
lsof -i :3306

# 检查Redis
redis-cli ping  # 应返回PONG

# 检查Milvus
curl http://localhost:19530/healthz

# 检查Neo4j
curl http://localhost:7474
```

---

## 二、最小化启动方案（仅核心功能）

如果Milvus/Neo4j/Ollama未安装，可先启动核心功能测试。

### 2.1 启动MySQL和Redis

**MySQL启动**：
```bash
# Windows
net start MySQL80  # 或 MySQL57，取决于版本

# Linux/Mac
sudo systemctl start mysql
# 或
brew services start mysql
```

**Redis启动**：
```bash
# Windows
redis-server

# Linux/Mac
sudo systemctl start redis
# 或
brew services start redis
```

### 2.2 确认数据库已创建

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库（如果不存在）
CREATE DATABASE IF NOT EXISTS wealth_butler CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出
exit;
```

### 2.3 检查.env配置文件

确认 `.env` 文件存在并配置正确：
```bash
# 查看.env文件
cat .env  # Linux/Mac
type .env  # Windows
```

**最小化配置**（仅MySQL和Redis）：
```ini
# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=wealth_butler
DB_CHARSET=utf8mb4

# Redis配置
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# LLM配置（通义千问）
MY_DASHSCOPE_API_KEY=你的通义千问API密钥
QWEN_DEFAULT_MODEL=qwen-plus

# JWT配置
JWT_SECRET=随机生成的密钥
AUTH_ACCESS_TOKEN_EXPIRE_MINUTES=30
```

---

## 三、启动后端服务

### 3.1 方式1：直接运行main.py（推荐）

```bash
cd D:\lqh\金融
python app/WealthButler/main.py
```

### 3.2 方式2：使用uvicorn命令

```bash
cd D:\lqh\金融
uvicorn app.WealthButler.main:app --host 0.0.0.0 --port 8010 --reload
```

### 3.3 启动成功标志

看到以下日志说明启动成功：
```
[Startup] 启动 EventBus 消费者...
[EventBus] Starting all consumers...
[EventBus] Started consumer: risk-worker-1 for stream:large_transaction in group risk_monitor_group
[EventBus] Started consumer: risk-worker-2 for stream:suspicious_intent in group risk_monitor_group
[EventBus] All 5 consumers started successfully
[Startup] 注册定时任务...
[Startup] 智能财富管家系统启动完成
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8010 (Press CTRL+C to quit)
```

---

## 四、访问系统

### 4.1 API文档（Swagger）
```
http://localhost:8010/docs
```
可以看到所有API接口，并进行在线测试。

### 4.2 前端登录页
```
http://localhost:8010/wealth/login
```

### 4.3 测试账号

**客户账号**：
- 用户名：`customer0001`
- 密码：`123456`

**理财顾问**：
- 用户名：`advisor001`
- 密码：`123456`

**数据分析师**：
- 用户名：`analyst001`
- 密码：`123456`

**风控专员**：
- 用户名：`risk001`
- 密码：`123456`

**运营专员**：
- 用户名：`operator001`
- 密码：`123456`

---

## 五、测试核心功能

### 5.1 测试登录功能

**方式1：通过前端页面**
1. 访问 http://localhost:8010/wealth/login
2. 输入测试账号：`customer0001` / `123456`
3. 点击登录

**方式2：通过API直接测试**
```bash
curl -X POST "http://localhost:8010/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"customer0001","password":"123456"}'
```

### 5.2 测试数据分析Agent（NL2SQL）

**前提条件**：需要登录并获取token

```bash
# 1. 登录获取token
TOKEN=$(curl -X POST "http://localhost:8010/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"analyst001","password":"123456"}' | jq -r '.data.access_token')

# 2. 测试NL2SQL查询
curl -X POST "http://localhost:8010/api/chat/analyst" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "统计客户总数",
    "session_id": "test-session-001"
  }'
```

### 5.3 测试持仓查询API

```bash
# 查询客户持仓
curl -X GET "http://localhost:8010/api/wealth/holdings?customer_id=1" \
  -H "Authorization: Bearer $TOKEN"
```

### 5.4 测试风险预警API

```bash
# 查询风险预警列表
curl -X GET "http://localhost:8010/api/risk/alerts?status=待处理" \
  -H "Authorization: Bearer $TOKEN"
```

---

## 六、测试5个Agent对话功能

### 6.1 测试客服Agent（需要Milvus）

```bash
curl -X POST "http://localhost:8010/api/chat/customer" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "货币基金的收益率是多少？",
    "session_id": "test-session-002"
  }'
```

**功能**：RAG知识库检索（FAQ/产品/政策）

### 6.2 测试投顾Agent（需要Neo4j）

```bash
curl -X POST "http://localhost:8010/api/chat/advisor" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "帮我推荐适合的理财产品",
    "session_id": "test-session-003",
    "customer_id": 1
  }'
```

**功能**：GraphRAG融合推荐 + 适当性匹配

### 6.3 测试数据分析Agent

```bash
curl -X POST "http://localhost:8010/api/chat/analyst" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "查询所有客户的平均年龄",
    "session_id": "test-session-004"
  }'
```

**功能**：NL2SQL自然语言转SQL查询

### 6.4 测试业务操作Agent

```bash
curl -X POST "http://localhost:8010/api/chat/operator" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "我要申购产品1，金额5000元",
    "session_id": "test-session-005",
    "customer_id": 1
  }'
```

**功能**：意图识别 + 参数提取 + 二次确认

### 6.5 测试风控Agent

风控Agent主要通过EventBus被动触发，可以通过以下方式测试：

```bash
# 触发大额交易事件（需要Redis）
# 风控Agent会自动消费事件并评估规则
```

---

## 七、常见启动问题排查

### 7.1 端口8010被占用

**报错**：`Address already in use`

**解决**：
```bash
# 查找占用进程
netstat -ano | findstr 8010  # Windows
lsof -i :8010                # Linux/Mac

# 修改启动端口
uvicorn app.WealthButler.main:app --port 8011
```

### 7.2 MySQL连接失败

**报错**：`Database connection failed`

**检查步骤**：
1. 确认MySQL服务已启动
2. 检查`.env`中的数据库配置
3. 测试连接：`mysql -h 127.0.0.1 -u root -p`
4. 确认数据库`wealth_butler`存在

### 7.3 Redis连接失败

**报错**：`Redis connection failed`

**检查步骤**：
1. 确认Redis服务已启动
2. 测试连接：`redis-cli ping`（应返回PONG）
3. 检查`.env`中的Redis配置

### 7.4 导入错误

**报错**：`No module named 'Base'`

**解决**：
```bash
# 确认在项目根目录
cd D:\lqh\金融

# 设置PYTHONPATH
set PYTHONPATH=D:\lqh\金融  # Windows
export PYTHONPATH=D:\lqh\金融  # Linux/Mac
```

### 7.5 DBUtils警告

**警告**：`DBUtils 未安装，使用单连接模式`

**解决**（可选）：
```bash
pip install DBUtils
```
注：不安装也可以运行，只是性能稍差。

---

## 八、可选服务启动（完整功能）

### 8.1 启动Milvus（客服Agent需要）

**Docker方式**：
```bash
# 拉取Milvus镜像
docker pull milvusdb/milvus:latest

# 启动Milvus
docker run -d --name milvus_standalone \
  -p 19530:19530 \
  -p 9091:9091 \
  milvusdb/milvus:latest
```

**验证**：
```bash
curl http://localhost:19530/healthz
```

### 8.2 启动Neo4j（投顾Agent需要）

**Docker方式**：
```bash
# 启动Neo4j
docker run -d --name neo4j \
  -p 7474:7474 -p 7687:7687 \
  -e NEO4J_AUTH=neo4j/your_password \
  neo4j:latest
```

**验证**：
访问 http://localhost:7474

### 8.3 启动Ollama（RAG向量化需要）

**安装Ollama**：
- Windows: 下载安装包 https://ollama.ai/download
- Linux: `curl https://ollama.ai/install.sh | sh`

**拉取模型**：
```bash
ollama pull bge-m3
```

**验证**：
```bash
ollama list  # 应该看到bge-m3模型
```

---

## 九、启动流程总结

**最小化启动（仅核心功能）**：
1. ✅ 启动MySQL
2. ✅ 启动Redis
3. ✅ 检查`.env`配置
4. ✅ 运行 `python app/WealthButler/main.py`
5. ✅ 访问 http://localhost:8010/docs

**完整功能启动**：
1. ✅ 启动MySQL
2. ✅ 启动Redis
3. ✅ 启动Milvus
4. ✅ 启动Neo4j
5. ✅ 启动Ollama
6. ✅ 检查`.env`配置（包含所有服务）
7. ✅ 运行 `python app/WealthButler/main.py`
8. ✅ 访问 http://localhost:8010/docs

---

## 十、下一步操作

启动成功后，可以进行：

1. **前端页面测试**
   - 访问各个工作台（客户/顾问/分析师/风控/运营）
   - 测试对话功能
   - 测试数据查询

2. **API接口测试**
   - 在Swagger UI中测试各个接口
   - 使用Postman/curl测试完整业务流程

3. **性能测试**
   - 测试响应时间（RAG<2s、NL2SQL<3s）
   - 测试并发能力

4. **端到端测试**
   - 测试完整业务闭环
   - 测试跨Agent事件流

---

**快速启动命令（一键启动）**：
```bash
cd D:\lqh\金融 && python app/WealthButler/main.py
```

**祝测试顺利！** 🚀
